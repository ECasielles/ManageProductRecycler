<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sombra -->
    <item>
        <shape>
            <solid android:color="@color/colorSombra" />
            <corners android:radius="4dp" />
        </shape>
    </item>

    <!-- Contenido -->
    <!-- Para que salga la sombra-->
    <item android:bottom="4dp">
        <shape>
            <solid android:color="@color/colorContenido" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</layer-list>